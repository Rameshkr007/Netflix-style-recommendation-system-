import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  {
    rules: {
      // This experimental rule flags the standard "setLoading(true) then
      // fetch" pattern used for data-fetching effects throughout this app.
      // That pattern is the conventional, correct way to fetch on mount in
      // React -- downgraded to a warning rather than disabled outright, so
      // it still surfaces genuinely risky cascading-render cases elsewhere.
      "react-hooks/set-state-in-effect": "warn",
    },
  },
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;
